[phases.setup]
nixPkgs = ["python313", "nodejs_22", "npm"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "cd dashboard && npm install"
]

[phases.build]
cmds = ["cd dashboard && npm run build"]

[start]
cmd = "uvicorn src.dashboard.app:app --host 0.0.0.0 --port ${PORT:-8000}"
